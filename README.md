# Beamforming Toy Model

This code is a simple toy model to observe what happens to Sum Rate when the angle is decreased between two UE's being served by SDMA beamforming.

![](animated_heatmap_annotated.gif)